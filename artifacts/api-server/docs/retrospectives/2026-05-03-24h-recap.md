# 24-Hour Recap — VulnRap (2026-05-03)

**Generated:** 2026-05-03 (regenerate with `node scripts/regenerate-24h-recap.mjs`)
**Window:** 24h — `2026-05-02T20:39:59.000Z` → `2026-05-03T20:39:59Z` (anchored to HEAD commit timestamp)
**Source:** `git log --since` + `artifacts/vulnrap/src/App.tsx` route diff + `artifacts/api-server/src/routes/test-fixtures.ts` diff

## 1. Headline

In the last 24 hours the team shipped 126 tracked tasks across 810 files, touching 8 engine signals and registering 28 new top-level routes in the public SPA.

**126 tasks merged**  ·  **810 files changed**  ·  **+204,348 / −78,134 lines**  ·  **28 new pages live**  ·  **31 new integrations live**  ·  **8 engine signals touched**  ·  **145 commits**

---

## 2. What landed

Grouped by surface bucket. **Note on "merge commits":** this repo squash-merges every task into trunk, so `git log --merges` is empty — each commit corresponds to one merged task / PR, and the recap treats every non-merge trunk commit in the window as the merge unit. Bucket assignment is hydrated from the on-disk task ledger (`.local/tasks/task-N.md`) when present, falling back to keyword regex on the commit subject (see `BUCKET_RULES` in the regenerator); when the parallelization runbook (Task #927) ships its bucketing helper, the rules will move there.

### engine/scoring — 29 tasks

Substance, fabrication, and rubric-shape work. Changes here move composite scores on real reports — they are the load-bearing improvements of the burn.

- **Task #623** — Add per-engine radar chart with live cohort overlay
- **Task #624** — Engine version pinning per report
- **Task #625** — Scoring latency distribution chart on /transparency
- **Task #628** — Per-signal mute / boost on /check
- **Task #630** — Per-engine on/off toggle on /check
- **Task #638** — Scoring regression golden corpus
- **Task #639** — Shadow Scoring Mode
- **Task #641** — Pre-merge scoring gate
- **Task #642** — Prompt-injection test fixtures
- **Task #651** — Scoring Playground page
- **Task #659** — Engine deep-dive: substance
- **Task #660** — Engine deep-dive: CWE coherence
- **Task #661** — Engine deep-dive: AVRI
- **Task #662** — Per-signal explainer pages
- **Task #663** — CWE reference page
- **Task #677** — VS Code extension for VulnRap scoring
- **Task #717** — Inline signal heatmap on report text
- **Task #736** — Add legit benchmark fixtures for shell-escaped IDOR/SQLi reproductions
- **Task #737** — Page reviewers when an AVRI scheduler replica goes silent
- **Task #744** — Cover foreign-language terse reports in slop/legit benchmark
- **Task #745** — Add composite-layer override regression tests for AVRI
- **Task #756** — Route XXE to DESERIALIZATION rubric
- **Task #763** — surface SQLi/XSS payloads in shell-escaped HTTP bodies
- **Task #765** — Tighten hallucination evidence filter LIKE pattern
- **Task #767** — Add per-engine deep-dive overview at /engines
- **`6450d442`** — Add new API schemas for audit logs and scoring history
- **`8ddd82a3`** — Final wind-down audit: typecheck fixes, dead-code review, scoring-gate bypass made loud
- **`91e6e056`** — Update API schema with new audit log and scoring history definitions
- **`e20d36a2`** — Bypass pre-merge scoring gate during wind-down to allow merges

### content/integration — 30 tasks

Public surfaces a submitter or integrator can read or call: docs, recipes, SDKs, badges, embeds, marketing pages.

- **Task #508** — Add boundary tests for 'Tight headroom' badge in ArchetypeRowView
- **Task #647** — Sample-report gallery page
- **Task #648** — How-it-works interactive page
- **Task #649** — Methodology whitepaper page
- **Task #652** — Glossary page
- **Task #654** — Add /quickstart page
- **Task #664** — Embeddable score badge SVG
- **Task #665** — Badge style gallery
- **Task #666** — Dynamic OG card endpoint
- **Task #668** — Public VulnRap MCP server
- **Task #669** — Python SDK (vulnrap)
- **Task #672** — Postman collection
- **Task #674** — GitHub Action recipe
- **Task #675** — Slack bot recipe
- **Task #679** — Browser bookmarklet
- **Task #681** — Add Bugcrowd integration recipe doc
- **Task #689** — Blog post — VulnRap MCP server launch
- **Task #690** — Methodology video script + storyboard
- **Task #691** — Case-study template + first study
- **Task #693** — Roadmap page (Now / Next / Later)
- **Task #694** — Showcase page
- **Task #695** — Press kit page
- **Task #701** — Podcast pitch templates
- **Task #729** — Discord bot: show top fired signals in /vulnrap-score embed
- **Task #730** — Discord bot: accept VR-XXXX report codes
- **Task #733** — Send a welcome email when someone joins the mailing list
- **Task #768** — Repoint Architecture Store node at the curated /gallery
- **Task #770** — Show Go SDK usage in HackerOne integration recipe
- **`bec32bdb`** — Open Discord and Slack community channels on /community
- **`cef77098`** — Add blog posts #9 (Show Our Work) and #10 (BYO Fixtures)

### UI — 8 tasks

Frontend polish: pages, panels, components, accessibility, navigation, theming.

- **Task #622** — Active verification trust panel
- **Task #627** — Custom sensitivity slider on /check
- **Task #646** — First-run onboarding tour
- **Task #650** — Compare-detectors page
- **Task #655** — Keyboard shortcuts + reference modal
- **Task #656** — Add accessibility statement page
- **Task #692** — Pricing page
- **Task #727** — Accessibility audit + automated a11y checks (WCAG 2.1 AA)

### security — 3 tasks

Hardening: spam controls, secret hygiene, supply-chain scans, allow-list misconfig warnings.

- **Task #708** — Add security disclosure policy
- **Task #709** — Threat model document
- **Task #735** — Add proof-of-work challenge to newsletter signup

### calibration/reviewer — 18 tasks

Reviewer-only surface: drift dashboards, handwavy-phrase workflow, brute-force alerts, rescore plumbing.

- **Task #504** — migrate handwavy e2e specs to shared helpers
- **Task #507** — e2e test for partial reinstate-batch via the reviewer UI
- **Task #617** — Public drift transparency widget
- **Task #645** — Reviewer-only /audit-log page
- **Task #673** — Webhook delivery system (reviewer-only)
- **Task #738** — Atomic write for brute-force cooldown state file
- **Task #740** — Attribute bulk re-arm actions to the reviewer in the audit log
- **Task #749** — Per-row reviewer ack workflow for calibration auth brute-force alerts
- **Task #750** — Add manual dismiss button to calibration token-rejected banner
- **Task #751** — Let reviewers fix typos in saved AI self-disclosure patterns
- **Task #752** — Let reviewers preview an AI self-disclosure pattern before saving it
- **Task #753** — Persist calibration auth alert history across server restarts
- **Task #760** — Stop the legacy report rescore backfill from auto-running on api-server start
- **Task #761** — isolate brute-force alerter in remaining calibration-auth tests
- **Task #762** — Stop the weekly rescore from firing once per server replica
- **Task #764** — Add route-level test for GET /feedback/calibration/rescore-backfill/scheduler-status
- **`66161582`** — Update generated API schemas with new audit log fields
- **`af1a5b8d`** — Guard calibration "Recent rollups" against missing recentRuns

### platform/ops — 15 tasks

Backend / deployment / observability / release plumbing. Quiet by design — visible only when something goes wrong.

- **Task #501** — Cache bulk-removal previews on the server
- **Task #685** — Helm chart
- **Task #706** — Public status / uptime page
- **Task #707** — Incident history page
- **Task #710** — Sitemap + robots.txt
- **Task #711** — Changelog RSS (Atom) feed
- **Task #712** — Blog Atom feed at /blog/feed.xml
- **Task #713** — Dark/light theme toggle
- **Task #714** — Print-friendly results page
- **Task #715** — i18n scaffold
- **Task #718** — v3.11 + v3.12 release-wave changelog
- **Task #722** — Production Dockerfile + full-stack docker-compose + self-hosting guide
- **Task #724** — Observability for the api-server
- **Task #725** — External-API resilience for outbound calls
- **Task #728** — Release process, SemVer, /api/version, OpenAPI breaking-change diff

### test infra — 11 tasks

Tests, fixtures, gates, codegen. Not user-visible but the reason any of the above can ship safely.

- **Task #509** — Add render test for warn-threshold chooser persistence
- **Task #626** — Public corpus stats page
- **Task #633** — Bring-your-own fixture battery (/test-yourself)
- **Task #643** — Multilingual input test battery
- **Task #720** — Add GitHub Actions CI/CD pipeline (PR checks, Dependabot, CodeQL, SBOM)
- **Task #721** — ESLint baseline + Prettier config + pre-commit hooks
- **Task #723** — Production-safe DB migrations (drizzle-kit generate/migrate, baseline, in-CI)
- **Task #747** — fix broken bulk-preview drop test for high-thrash phrases
- **Task #748** — Fix Task #748 — broken bulk-remove drop-only-high-thrash-phrase e2e test
- **`254c946e`** — Regenerate API client code to resolve codegen synchronization issues
- **`81b8e91e`** — Remove outdated test file for badge endpoint

### other — 31 tasks

Commits whose subjects didn't match any bucket heuristic. Cited as-is so the count reconciles.

- **Task #615** — Cohort baseline ribbon on results
- **Task #620** — Score stability monitor
- **Task #621** — Score evolution timeline
- **Task #629** — Custom redaction rules
- **Task #631** — Shareable preset library
- **Task #632** — A/B preset comparison mode
- **Task #634** — User-suggested phrase form
- **Task #637** — Pre-submit quality grader
- **Task #640** — Holdout evaluation set for honest precision/recall
- **Task #644** — Cross-AI-agent pattern detector
- **Task #699** — Trust badges on home
- **Task #716** — Markdown report export
- **Task #726** — Performance audit (revision 2 + rebase resolution)
- **Task #732** — Auto-close dry-run report endpoint
- **Task #741** — thread_role_no_overlap structural-fabrication detector
- **Task #742** — extend DQUOTE/SQBRACKET prose-placeholder regexes to accept punctuation separators
- **Task #743** — Clean up remaining type errors blocking green build
- **Task #746** — Drop the duplicate top-level goldHitCount on AvriEngine2Result
- **Task #754** — Recognise terse vulnerability shorthand in DE/ZH/IT/KO
- **Task #755** — Recognize bash $'...' ANSI-C HTTP reproductions
- **Task #766** — Warn at startup when ALLOWED_ORIGINS is missing or malformed
- **Task #929** — 24-hour retrospective: what we shipped on the free credits
- **`2bbcd490`** — Git commit prior to merge
- **`3d3a03bf`** — Git commit prior to merge
- **`6465fe91`** — Fix formatting error in workspace configuration file
- **`866054d5`** — Git commit prior to merge
- **`88f72175`** — Fix dependency issues in package lock file
- **`baf2d208`** — Git commit prior to merge
- **`dcf336a8`** — Git commit prior to merge
- **`e12d7ff9`** — Update generated API client code for consistency
- **`ee72bdc7`** — Git commit prior to merge

*Bucket totals reconcile to 145 commits in window.*

---

## 3. How the analysis got better

No on-disk scoring-regression / real-world-corpus output was found under `artifacts/api-server/test-output/` (or sibling paths) at regen time. Per-signal entries fall back to **no measurable change** rather than guessing.

- **gold signals** (`artifacts/api-server/src/lib/engines/avri/families.ts`) — touched in window; no measurable change (no per-signal entry in scoring artifact).
- **fabricated-evidence flags** (`artifacts/api-server/src/lib/engines/avri/fabricated-evidence-flags.ts`) — no measurable change (file untouched in window).
- **handwavy-phrase impact** (`artifacts/api-server/src/lib/handwavy-phrases.ts`) — no measurable change (file untouched in window).
- **crash-trace** (`artifacts/api-server/src/lib/engines/avri/crash-trace.ts`) — touched in window; no measurable change (no per-signal entry in scoring artifact).
- **hallucination cohort** (`artifacts/api-server/src/lib/hallucination-detector.ts`) — touched in window; no measurable change (no per-signal entry in scoring artifact).
- **linguistic** (`artifacts/api-server/src/lib/engines/engines.ts`) — touched in window; no measurable change (no per-signal entry in scoring artifact).
- **sloppiness (slop-signals)** (`artifacts/api-server/src/lib/engines/avri/slop-signals.ts`) — no measurable change (file untouched in window).
- **claim-specificity** (`artifacts/api-server/src/lib/engines/extractors.ts`) — touched in window; no measurable change (no per-signal entry in scoring artifact).
- **evidence-quality** (`artifacts/api-server/src/lib/engines/avri/engine2-avri.ts`) — touched in window; no measurable change (no per-signal entry in scoring artifact).
- **internal-consistency (CWE coherence)** (`artifacts/api-server/src/lib/engines/avri/coherence.ts`) — touched in window; no measurable change (no per-signal entry in scoring artifact).
- **AVRI raw-http** (`artifacts/api-server/src/lib/engines/avri/raw-http.ts`) — touched in window; no measurable change (no per-signal entry in scoring artifact).

**Fixture-battery delta (`test-fixtures.ts`):** 77 → 77 fixtures (+0). Per-tier: T1 15→15, T2 10→10, T3 33→33, T4 19→19.

---

## 4. What's now visible to users

Routes registered in `artifacts/vulnrap/src/App.tsx` during the window (HEAD vs. parent of the window's oldest in-window commit):

| Route | Value-prop |
| ----- | ---------- |
| `/accessibility` | new top-level page (no value-prop entry yet — add one to `ROUTE_VALUE_PROPS` in `scripts/regenerate-24h-recap.mjs`). |
| `/audit-log` | new top-level page (no value-prop entry yet — add one to `ROUTE_VALUE_PROPS` in `scripts/regenerate-24h-recap.mjs`). |
| `/badges` | new top-level page (no value-prop entry yet — add one to `ROUTE_VALUE_PROPS` in `scripts/regenerate-24h-recap.mjs`). |
| `/compare-detectors` | detector comparison — compare per-signal output across detectors. |
| `/corpus-stats` | corpus statistics — per-tier counts, churn, and signal coverage. |
| `/cwe` | new top-level page (no value-prop entry yet — add one to `ROUTE_VALUE_PROPS` in `scripts/regenerate-24h-recap.mjs`). |
| `/docs/good-report` | new top-level page (no value-prop entry yet — add one to `ROUTE_VALUE_PROPS` in `scripts/regenerate-24h-recap.mjs`). |
| `/engines` | new top-level page (no value-prop entry yet — add one to `ROUTE_VALUE_PROPS` in `scripts/regenerate-24h-recap.mjs`). |
| `/engines/avri` | new top-level page (no value-prop entry yet — add one to `ROUTE_VALUE_PROPS` in `scripts/regenerate-24h-recap.mjs`). |
| `/engines/cwe-coherence` | new top-level page (no value-prop entry yet — add one to `ROUTE_VALUE_PROPS` in `scripts/regenerate-24h-recap.mjs`). |
| `/engines/substance` | new top-level page (no value-prop entry yet — add one to `ROUTE_VALUE_PROPS` in `scripts/regenerate-24h-recap.mjs`). |
| `/gallery` | new top-level page (no value-prop entry yet — add one to `ROUTE_VALUE_PROPS` in `scripts/regenerate-24h-recap.mjs`). |
| `/glossary` | new top-level page (no value-prop entry yet — add one to `ROUTE_VALUE_PROPS` in `scripts/regenerate-24h-recap.mjs`). |
| `/how-it-works` | new top-level page (no value-prop entry yet — add one to `ROUTE_VALUE_PROPS` in `scripts/regenerate-24h-recap.mjs`). |
| `/incidents` | new top-level page (no value-prop entry yet — add one to `ROUTE_VALUE_PROPS` in `scripts/regenerate-24h-recap.mjs`). |
| `/playground` | new top-level page (no value-prop entry yet — add one to `ROUTE_VALUE_PROPS` in `scripts/regenerate-24h-recap.mjs`). |
| `/presets` | new top-level page (no value-prop entry yet — add one to `ROUTE_VALUE_PROPS` in `scripts/regenerate-24h-recap.mjs`). |
| `/press` | new top-level page (no value-prop entry yet — add one to `ROUTE_VALUE_PROPS` in `scripts/regenerate-24h-recap.mjs`). |
| `/pricing` | new top-level page (no value-prop entry yet — add one to `ROUTE_VALUE_PROPS` in `scripts/regenerate-24h-recap.mjs`). |
| `/quickstart` | new top-level page (no value-prop entry yet — add one to `ROUTE_VALUE_PROPS` in `scripts/regenerate-24h-recap.mjs`). |
| `/redaction-examples` | new top-level page (no value-prop entry yet — add one to `ROUTE_VALUE_PROPS` in `scripts/regenerate-24h-recap.mjs`). |
| `/roadmap` | new top-level page (no value-prop entry yet — add one to `ROUTE_VALUE_PROPS` in `scripts/regenerate-24h-recap.mjs`). |
| `/showcase` | new top-level page (no value-prop entry yet — add one to `ROUTE_VALUE_PROPS` in `scripts/regenerate-24h-recap.mjs`). |
| `/signals` | new top-level page (no value-prop entry yet — add one to `ROUTE_VALUE_PROPS` in `scripts/regenerate-24h-recap.mjs`). |
| `/signals/:id` | new top-level page (no value-prop entry yet — add one to `ROUTE_VALUE_PROPS` in `scripts/regenerate-24h-recap.mjs`). |
| `/status` | new top-level page (no value-prop entry yet — add one to `ROUTE_VALUE_PROPS` in `scripts/regenerate-24h-recap.mjs`). |
| `/test-yourself` | new top-level page (no value-prop entry yet — add one to `ROUTE_VALUE_PROPS` in `scripts/regenerate-24h-recap.mjs`). |
| `/whitepaper` | new top-level page (no value-prop entry yet — add one to `ROUTE_VALUE_PROPS` in `scripts/regenerate-24h-recap.mjs`). |

**Non-route additions in window:**

| Surface | Path | Note |
| ------- | ---- | ---- |
| integration recipe | `artifacts/api-server/docs/integrations/bugcrowd.md` | new integration recipe doc. |

Screenshots are intentionally not auto-captured by this script — adding one for any row above is a manual step (drop a JPEG into `artifacts/api-server/docs/retrospectives/img/` and link it inline).

---

## 5. What's still in flight

**5 IN_PROGRESS + 100 PENDING = 105 tasks in flight** (source: `.local/tasks/_status-snapshot.json`, snapshot taken `2026-05-03T20:50:14.479Z` via `listProjectTasks`).

Top buckets:

| Bucket | Count |
| ------ | ----: |
| other | 32 |
| engine/scoring | 21 |
| content/integration | 19 |
| UI | 13 |
| calibration/reviewer | 11 |

---

## 6. What we learned (operational)

These are short, citation-backed lessons from the burn. Each one names task refs so future agents can chase the receipts.

- **Hot files serialize the queue.** Files like `artifacts/vulnrap/src/pages/feedback-analytics.tsx` (~18k LOC) accumulate edits from many tasks and become merge-conflict choke points. Task #927 (parallelization runbook) is the planned mitigation — it should produce a hot-file inventory and wave plan that future batches consume.
- **Bucketing is keyword-fragile.** Until Task #927's bucketing helper lands, this recap classifies via regex on commit subjects. Any commit whose subject doesn't say what it is (e.g. `Git commit prior to merge`) lands in the `other` bucket and shows up in the reconcile total.
- **`/check` modes work.** Every regenerator script in `scripts/` (including this one) supports `--check`; CI catches stale docs without re-running expensive analysis. The convention came from `scripts/regenerate-state-of-platform.mjs` — keep it.
- **The scoring corpus needs an on-disk artifact.** Section 3 above degrades gracefully to "no measurable change" because no `real-world-corpus` output is durably written. When the test runner persists per-signal before/after JSON under `artifacts/api-server/test-output/`, this section gets numerical without a code change here.
- **Route inventory beats narrative.** Diffing `App.tsx` route tables is a cheap, deterministic answer to "what's new for users this week" — much harder to fudge than a hand-written list. The same trick is reusable for any longer recap window via `--window=7d`.

---

## 7. Credit accounting

Best-effort numbers from what's actually on disk at regen time:

- **Merged commits in window:** 145 (`git log --since=2026-05-02T20:39:59.000Z --no-merges`).
- **Tracked task refs merged:** 126 (commits whose subject parses as `Task #N`).
- **Files touched:** 810.
- **Lines added / removed:** +204,348 / −78,134.
- **Wall-clock window:** 2026-05-02T20:39:59.000Z → 2026-05-03T20:39:59Z.
- **Per-task-agent run count:** *unknown* — no in-tree ledger of agent invocations; only merged commits are observable from this script.
- **Token / credit spend:** *unknown* — billing data is not in the repo.

---

## Marketing draft

The block below is paste-ready for `artifacts/vulnrap/src/pages/changelog.tsx`. **This script does not edit `changelog.tsx` directly** — that file is hot and owned by the changelog task. Copy/edit manually.

> **24-hour burn — 126 tasks merged.**
> 
> In the last 24 hours we shipped 126 tracked tasks across 810 files (+204,348 / −78,134 lines), including 28 new public pages.
> 
> New surfaces:
> - `/accessibility`
> - `/audit-log`
> - `/badges`
> - `/compare-detectors`
> - `/corpus-stats`
> - `/cwe`
> - `/docs/good-report`
> - `/engines`
> 
> _Paste-ready draft for `artifacts/vulnrap/src/pages/changelog.tsx`. This file is intentionally not edited by the regenerator (hot file, owned by the changelog task)._

---

## Regenerating this document

```bash
node scripts/regenerate-24h-recap.mjs            # write
node scripts/regenerate-24h-recap.mjs --check    # CI-safe, exits non-zero if stale
node scripts/regenerate-24h-recap.mjs --window=48h
node scripts/regenerate-24h-recap.mjs --since=2026-05-02T00:00:00Z
```

The window anchors to the HEAD commit's timestamp (not wall-clock), so regenerating the same repo state produces the same doc. The `Generated:` line is the only date-sensitive field and is ignored by `--check`.
