apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "vulnrap.fullname" . }}
  labels:
    {{- include "vulnrap.labels" . | nindent 4 }}
data:
  {{- range $key, $val := .Values.env }}
  {{ $key }}: {{ $val | quote }}
  {{- end }}
