{
  "name": "@workspace/scripts",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "hello": "tsx ./src/hello.ts",
    "generate:postman": "node ./generate-postman.mjs",
    "generate:bookmarklet": "node ../sdks/bookmarklet/generate.mjs",
    "typecheck": "tsc -p tsconfig.json --noEmit && node check-artifact-paths.test.mjs",
    "test": "node check-artifact-paths.test.mjs"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "openapi-to-postmanv2": "^6.0.1",
    "tsx": "catalog:"
  }
}
