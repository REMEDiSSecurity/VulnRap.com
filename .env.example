DATABASE_URL=postgresql://user:password@localhost:5432/vulnrap

# Optional: comma-separated list of allowed CORS origins (defaults to allow all in dev)
# ALLOWED_ORIGINS=https://vulnrap.com,https://www.vulnrap.com

# Optional: public-facing URL for verification links (defaults to request origin)
# PUBLIC_URL=https://vulnrap.com
